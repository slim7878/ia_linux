services:

  # --- Core ---

  llamacpp:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: llamacpp
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./models:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: >
      --models-dir /models/
      --models-max 1
      --n-gpu-layers 99
      --parallel 2
      --flash-attn auto
      --ctx-size 8192
      --mmap
      --cache-type-k q8_0
      --cache-type-v q8_0
      --jinja
      --reasoning-budget 0
      --temp 0.8
      --top-p 0.95
      --top-k 40
      --min-p 0.05
      --presence-penalty 1.1
      --repeat-penalty 1.1
      --sleep-idle-seconds 30
      --no-ui
      --port 8080
      --host 0.0.0.0

  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: openwebui
    restart: always
    ports:
      - "127.0.0.1:3000:8080"
    volumes:
      - openwebui_data:/app/backend/data
    environment:
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      OPENAI_API_BASE_URL: http://llamacpp:8080/v1
      OPENAI_API_KEY: none
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - llamacpp

  hermes:
    image: nousresearch/hermes-agent:latest
    container_name: hermes
    restart: unless-stopped
    command: gateway run
    ports:
      - "127.0.0.1:8642:8642"
      - "127.0.0.1:9119:9119"
    volumes:
      - ~/.hermes:/opt/data
    environment:
      HERMES_DASHBOARD: "1"
      HERMES_DASHBOARD_BASIC_AUTH_USERNAME: ${HERMES_DASHBOARD_USER}
      HERMES_DASHBOARD_BASIC_AUTH_PASSWORD: ${HERMES_DASHBOARD_PASSWORD}
      HERMES_DASHBOARD_BASIC_AUTH_SECRET: ${HERMES_DASHBOARD_SECRET}
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_PORT: "8642"
      API_SERVER_KEY: ${HERMES_API_KEY}
      API_SERVER_CORS_ORIGINS: "'*'"
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_ALLOWED_USERS: ${TELEGRAM_ALLOWED_USERS}
      TELEGRAM_HOME_CHANNEL: ${TELEGRAM_HOME_CHANNEL:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CLICKUP_API_TOKEN: ${CLICKUP_API_TOKEN:-}
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - llamacpp

  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      DOCKER_API_VERSION: "1.41"
    command: --interval 86400 --cleanup

  # --- Optionnels (activer avec : docker compose --profile <nom> up -d) ---

  # Qwen3.8-27B via vLLM, optimisé RTX 3090 (port 18020)
  # ATTENTION : incompatible avec llamacpp en simultané — arrêter llamacpp avant de lancer ce profil.
  # Premier démarrage : téléchargement + requantization du modèle (~20 Go, 20-40 min).
  # Variables d'environnement (toutes optionnelles, voir .env.example) :
  #   QWEN38_API_KEY    — clé d'auth pour l'API (laisser vide = pas d'auth)
  #   QWEN38_SPEC       — moteur de spéculation : "" (MTP, défaut) ou "dflash2" (recommandé solo)
  #   QWEN38_PREFIX_CACHE — "1" pour activer le cache de préfixe (recommandé solo)
  #   QWEN38_DFLASH_TOKENS — nb de tokens draft par étape : "7" (défaut) ou "15" (si réponses citent le prompt)
  qwen38:
    image: ghcr.io/syv-ai/qwen38-27b-rtx3090:latest
    container_name: qwen38
    profiles: [qwen38]
    restart: unless-stopped
    ports:
      - "127.0.0.1:18020:18020"
    volumes:
      - qwen38_models:/app/models
      - qwen38_cache:/cache
    ipc: host
    environment:
      VLLM_API_KEY: ${QWEN38_API_KEY:-}
      SPEC: ${QWEN38_SPEC:-}
      PREFIX_CACHE: ${QWEN38_PREFIX_CACHE:-}
      DFLASH_TOKENS: ${QWEN38_DFLASH_TOKENS:-}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  comfyui:
    image: ghcr.io/ai-dock/comfyui:latest-cuda
    container_name: comfyui
    profiles: [comfyui]
    restart: unless-stopped
    ports:
      - "127.0.0.1:7860:7860"
    volumes:
      - comfyui_data:/opt/ComfyUI
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  whisper:
    image: onerahmet/openai-whisper-asr-webservice:latest-gpu
    container_name: whisper
    profiles: [whisper]
    restart: unless-stopped
    ports:
      - "127.0.0.1:9000:9000"
    environment:
      ASR_MODEL: base
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  openwebui_data:
  comfyui_data:
  qwen38_models:
  qwen38_cache:
